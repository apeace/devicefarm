# this config is missing devicegroups

defaults:
    build:
      - echo "Foo"
      - echo "Bar"

    android:
      apk: ./path/to/build.apk
      apk_instrumentation: ./path/to/instrumentation.apk

    devicegroups:
      - a_few_devices

branches:
  master:
    devicegroups:
      - everything
