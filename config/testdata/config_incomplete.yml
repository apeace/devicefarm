# this config is missing devicepool_definitions

defaults:
    build:
      - echo "Foo"
      - echo "Bar"

    android:
      apk: ./path/to/build.apk
      apk_instrumentation: ./path/to/instrumentation.apk

    devicepool: a_few_devices

branches:
  master:
    devicepool: everything
